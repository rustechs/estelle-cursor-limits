# Raise inotify watch limit for Cursor file watchers (home-as-workspace, many worktrees).
# Rendered at install time from CURSOR_INOTIFY_MIN_WATCHES (default 524288).
fs.inotify.max_user_watches=@MAX_USER_WATCHES@
